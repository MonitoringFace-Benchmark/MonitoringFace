experiment_name: GDPR_case_study

monitors:
  - identifier: EnfGuard
    name: EnfGuard
    branch: enfguard

repeats: 3

runtime_constraints:
  upper_bound: 30

# Case study specific configuration
data_setup:
  type: CaseStudy
  name: GDPR_EnfGuard

# Tools to build and run
tools_to_build:
  - EnfGuard
