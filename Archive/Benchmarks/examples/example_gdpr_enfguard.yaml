experiment_name: GDPR_case_study

benchmark_type: case_study
experiment_type: CaseStudy

# Tools configuration
tools:
  - name: EnfGuard
    branch: enfguard
    release: branch

# Monitors configuration
monitors:
  - identifier: EnfGuard
    name: EnfGuard
    branch: enfguard
    params:
      replayer: gen_data
      preprocessing: false

repeats: 3

time_guard:
  enabled: true
  lower_bound: null
  upper_bound: 30
  guard_type: Monitor
  guard_name: Guard

# Case study specific configuration
case_study_config:
  case_study_name: GDPR_EnfGuard

# Tools to build and run
tools_to_build:
  - EnfGuard
